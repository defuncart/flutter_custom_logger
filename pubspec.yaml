name: flutter_custom_logger
description: A custom logger for Flutter
publish_to: 'none'
version: 0.0.1
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logger: 0.7.0
  logger_flutter:
    git:
      url: https://github.com/defuncart/fork_logger_flutter.git
      ref: feature/add-open-console-on-box-gesture

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_analysis_options:
    git:
      url: https://github.com/defuncart/flutter_analysis_options.git
